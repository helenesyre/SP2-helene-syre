import { createIcons, Menu, CirclePlus, CircleDollarSign, X, User, Home, LogIn, LogOut, CircleCheck, AlertCircle, Info } from 'lucide';

export function renderIcons() {
  createIcons({
    icons: { Menu, CirclePlus, CircleDollarSign, X, User, Home, LogIn, LogOut, CircleCheck, AlertCircle, Info }
  });
}
